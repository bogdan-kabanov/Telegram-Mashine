services:
  app:
    build:
      context: .
      args:
        PLAYWRIGHT_VERSION: "1.61.1"
        BASE_PATH: "${BASE_PATH:-/ai}"
    image: bot-ai:latest
    container_name: bot-ai
    restart: unless-stopped
    ports:
      - "127.0.0.1:${PORT:-3002}:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      DOCKER: "1"
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
      DATA_DIR: /app/data
      CONFIG_DIR: /app/config
      BASE_PATH: "${BASE_PATH:-/ai}"
      NEXT_PUBLIC_BASE_PATH: "${BASE_PATH:-/ai}"
      PLAYWRIGHT_BROWSERS_PATH: /ms-playwright
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: "1"
      # Set to 1 to auto-register Telegram webhook on start
      AUTO_SETUP_WEBHOOK: "${AUTO_SETUP_WEBHOOK:-0}"
    volumes:
      # Persist DB, media uploads, reviews, legends
      - ./data:/app/data
      # Persist project/schedule config edits
      - ./config:/app/config
      # Persist generated PNG previews
      - ./public/renders:/app/public/renders
    # Chromium needs shared memory — keep modest on 2GB VPS
    shm_size: "256mb"
    # This VPS blocks some Telegram DC IPs (e.g. 149.154.166.110); pin a reachable one
    extra_hosts:
      - "api.telegram.org:149.154.167.220"
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "const b=(process.env.NEXT_PUBLIC_BASE_PATH||process.env.BASE_PATH||'').replace(/\\/$/,'');fetch('http://127.0.0.1:3000'+b+'/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
